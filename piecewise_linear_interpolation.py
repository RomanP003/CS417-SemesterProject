# core number coresponds to colum

# readings taken every 30 seconds, time corresponds to rows 

def piecewise_linear_interpolation(times, core_data):
    # x_k and x_k+1 is equivalent to the time a reading is taken
    # y_k and y_k+1 are equivalent to core temps at different times
    #for (x0, x1), (y0, y1) in list(zip(times[-1:], times[1:]), zip(core_data[-1:], core_data[1:])):
        #slope = (y1 - y0) / (x1 - x0)

       #b = y1 - slope * x1
                


    #coefficients = (b, slope)

    return times